# Colab requirements for the revision-robustness notebooks.
# The ladder is sklearn's HistGradientBoosting + logistic/ridge/OLS - no
# xgboost/lightgbm anywhere in the pipeline (checked against the imports of
# e1_baselines, e9a/e9b/e10/e14, r1/r2/r6, e12_corrected_aggregation).
#
# torch: use the runtime's preinstalled CUDA build (Colab A100 ships a
# cu12x wheel); do NOT reinstall torch here - torch-geometric below is a
# pure-python package since 2.3 and works against any torch >= 2.1.
#
# sklearn: the frozen pipeline ran 1.2.2 locally. 1.2.2 has no wheels for
# python >= 3.12, so newer runtimes take the closest supported line; the
# theta = 0.20 cell of the sweep doubles as the calibration anchor that
# detects any version-induced drift (compare against the frozen e1 numbers
# in results/results_<field>/e1_baselines.json).
scikit-learn==1.2.2; python_version < "3.12"
scikit-learn>=1.3,<1.8; python_version >= "3.12"
scipy>=1.10,<2
pandas>=1.5,<3
numpy>=1.24,<3
pyarrow>=12
requests>=2.31
torch-geometric==2.8.0
